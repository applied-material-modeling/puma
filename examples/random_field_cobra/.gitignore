# CoBRA pipeline outputs (regenerated by `python generate.py`)
output/
cobra.log

# Generated PUMA initial condition (regenerate from the committed data/scans.npy)
initial_condition.csv

# Keep committed:  data/scans.npy  (small CT-derived field, for reproducibility)
#                  porosity_field.png  (documentation preview)

# Re-include the preview PNG (the repo root .gitignore ignores *.png globally)
!porosity_field.png
