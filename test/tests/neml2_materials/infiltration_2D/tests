[Tests]
  [test]
    type = 'Exodiff'
    input = 'input.i'
    exodiff = 'input_out.e'
    # phif is physically zero over most of the domain early on; the only diffs vs
    # the 2.x gold are floating-point roundoff on those ~1e-17 values. Floor them.
    abs_zero = 1e-8
    requirement = 'Make sure output is the same.'
  []
[]
